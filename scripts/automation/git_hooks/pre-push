#!/usr/bin/env bash
# Git hook: pre-push
# Runs scope-aware formatting, linting, and tests before push
# Bypass: SKIP_PRE_PUSH=1 git push ...

# Allow bypass for emergency situations
if [[ "${SKIP_PRE_PUSH:-}" == "1" ]]; then
  echo "⚠️  Pre-push checks skipped (SKIP_PRE_PUSH=1)"
  exit 0
fi

echo "🔍 Running pre-push checks..."
echo ""

# Get the root of the git repository
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT" || exit 1

# Get commit messages from commits being pushed
UPSTREAM_BRANCH="$(git rev-parse --abbrev-ref --symbolic-full-name @{u} 2>/dev/null || echo "")"
if [[ -z "$UPSTREAM_BRANCH" ]]; then
  echo "⚠️  No upstream branch detected. Falling back to origin/dev for scope detection."
  UPSTREAM_BRANCH="origin/dev"
fi

commits=$(git log "$UPSTREAM_BRANCH"..HEAD --format=%B 2>/dev/null)

# Extract crate names from commit scopes
detect_crates_from_scopes() {
  local crates=()
  local invalid_scopes=0
  local scope_re='^[a-z]+\(([^)]+)\):'

  # Extract scope from "type(scope): message" format
  # scope can be: products/accounts/backend → accounts-backend
  # or: products/core/engine → engine
  # or: scripts/... → (skip, not a crate)
  while IFS= read -r line; do
    # Match pattern: type(scope):
    if [[ $line =~ $scope_re ]]; then
      local scope="${BASH_REMATCH[1]}"

      # Parse scope: products/accounts/backend → accounts-backend
      if [[ $scope =~ ^(libraries|products)/([^/]+)(/([^/]+))?$ ]]; then
        local crate_path="${BASH_REMATCH[2]}"
        if [[ -n "${BASH_REMATCH[4]}" ]]; then
          crate_path="${crate_path}/${BASH_REMATCH[4]}"
        fi

        # Validate that crate exists in Cargo.toml (has Cargo.toml file)
        local cargo_toml="projects/$scope/Cargo.toml"
        if [[ ! -f "$cargo_toml" ]]; then
          # Crate doesn't exist
          invalid_scopes=$((invalid_scopes + 1))
        else
          # Resolve actual crate name from Cargo.toml
          local crate_name
          crate_name=$(sed -n 's/^name[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' "$cargo_toml" | head -n1)
          if [[ -z "$crate_name" ]]; then
            invalid_scopes=$((invalid_scopes + 1))
            continue
          fi

          # Avoid duplicates
          if [[ ! " ${crates[@]} " =~ " ${crate_name} " ]]; then
            crates+=("$crate_name")
          fi
        fi
      fi
    fi
  done <<< "$commits"

  # If there were invalid scopes, return empty (force full workspace test)
  if [[ $invalid_scopes -gt 0 ]]; then
    return 1
  fi

  printf '%s\n' "${crates[@]}"
}

# Get affected crates from commit scopes
AFFECTED=$(detect_crates_from_scopes)
SCOPE_VALID=$?

if [[ $SCOPE_VALID -ne 0 ]] || [[ -z "$AFFECTED" ]]; then
  echo "⚠️  Invalid or missing crate scope(s) in commits - testing full workspace"
  echo "   (Use scopes like 'refactor(products/accounts/backend):' pointing to existing crates)"
  echo ""
  CLIPPY_ARGS="--all-targets --all-features"
  TEST_ARGS="--workspace"
else
  echo "🎯 Crates from commit scopes:"
  echo "$AFFECTED" | sed 's/^/   - /'
  echo ""

  # Build clippy/test args for affected crates
  CLIPPY_ARGS=""
  TEST_ARGS=""
  while IFS= read -r crate; do
    [[ -z "$crate" ]] && continue
    CLIPPY_ARGS+=" -p $crate"
    TEST_ARGS+=" -p $crate"
  done <<< "$AFFECTED"
fi

# Track overall status
FAILED=0

# 1. Check formatting
echo "📝 Checking code formatting..."
if ! cargo fmt --all --check; then
  echo ""
  echo "❌ Code formatting check failed!"
  echo "   Run: cargo fmt --all"
  echo ""
  FAILED=1
else
  echo "✅ Formatting OK"
  echo ""
fi

# 2. Run clippy on affected crates
echo "🔎 Running clippy..."
# shellcheck disable=SC2086
if ! cargo clippy $CLIPPY_ARGS -- -D warnings; then
  echo ""
  echo "❌ Clippy found issues!"
  echo "   Run: cargo clippy $CLIPPY_ARGS -- -D warnings"
  echo ""
  FAILED=1
else
  echo "✅ Clippy OK"
  echo ""
fi

# 3. Run tests on affected crates
echo "🧪 Running tests..."
# shellcheck disable=SC2086
if ! cargo test $TEST_ARGS; then
  echo ""
  echo "❌ Tests failed!"
  echo "   Run: cargo test $TEST_ARGS"
  echo ""
  FAILED=1
else
  echo "✅ Tests OK"
  echo ""
fi

# Final result
if [[ $FAILED -eq 1 ]]; then
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "❌ Pre-push checks FAILED"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Please fix the issues before pushing."
  echo "Bypass (emergency only): SKIP_PRE_PUSH=1 git push"
  echo ""
  exit 1
fi

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ All pre-push checks PASSED"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

exit 0
