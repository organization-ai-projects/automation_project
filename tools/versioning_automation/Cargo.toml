[package]
name = "versioning_automation"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true, features = ["derive"] }
regex = "1.11"
common_json = { workspace = true }
protocol = { workspace = true }
