# platform_ide

Stable IDE product with strict `backend/ui` crate layout.

This product provides:

- `platform_ide_backend`: API-facing backend workflow and verification orchestration.
- `platform_ide_ui`: user-facing interface flow and local state views.
