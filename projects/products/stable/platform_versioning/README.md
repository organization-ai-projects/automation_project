# platform_versioning

Stable product workspace with two bin-only crates:

- `platform_versioning_backend`
- `platform_versioning_ui`

This product follows repository contract rules enforced by `repo_contract_enforcer`.
