#[allow(dead_code)]
pub fn render_config_screen() -> Vec<String> {
    vec![
        "=== Config Screen ===".to_string(),
        "Enter seed, population size, max generations, rule pool.".to_string(),
    ]
}
