[package]
name = "example_report_generator"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = "1.0"

# Note: This is an MVP example. Not included in workspace until needed.
# To add to workspace, update root Cargo.toml members list.
