[package]
name = "invalid_unstable_missing_manifests_ui"
version = "0.1.0"
edition = "2024"

[dependencies]

[target.'cfg(target_arch = "wasm32")'.dependencies]
dioxus = { version = "0.7.3", features = ["web"] }
