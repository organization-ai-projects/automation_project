# colony_manager

A deterministic colony simulation engine.

## Usage

```
colony_manager run --ticks N --seed S [--scenario <path>] --out <path> [--replay-out <path>]
colony_manager replay --replay <path> --out <path>
```
